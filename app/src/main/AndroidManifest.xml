<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zulfikar.utstimelinepribadi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UpdateTimelineActivity" android:label="Update Timeline"></activity>
        <activity android:name=".TimelineActivity" android:label="Timeline Pribadi"/>
        <activity android:name=".DetailActivity" android:label="Detail Timeline"/>
        <activity android:name=".AddTimelineActivity" android:label="Add Timeline"/>
        <activity android:name=".MainActivity" android:theme="@style/LoginRegisterTheme">
        </activity>
        <activity android:name=".Splash.SplashActivity" android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>